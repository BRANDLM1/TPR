{
  "name": "tpr-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "The main workspace for the TPR project, containing both the frontend and backend applications.",
  "workspaces": [
    "packages/frontend",
    "packages/backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=frontend\" \"npm run dev --workspace=backend\"",
    "build:frontend": "npm run build --workspace=frontend",
    "build:backend": "npm run build --workspace=backend",
    "install:all": "npm install --workspace=frontend && npm install --workspace=backend",
    "db:migrate": "dotenv -- npx prisma migrate dev --schema=./packages/backend/prisma/schema.prisma",
    "db:generate": "dotenv -- npx prisma generate --schema=./packages/backend/prisma/schema.prisma",
    "db:studio": "dotenv -- npx prisma studio --schema=./packages/backend/prisma/schema.prisma",
    "db:pull": "dotenv -- npx prisma db pull --schema=./packages/backend/prisma/schema.prisma"
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "dotenv-cli": "^10.0.0",
    "eslint": "^8.0.0",
    "postcss": "^8.5.6",
    "postcss-preset-mantine": "^1.18.0",
    "postcss-simple-vars": "^7.0.1",
    "prettier": "^3.0.0",
    "typescript": "^5.8.3"
  }
}
